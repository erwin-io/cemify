import { WorkOrderPipe } from './work-order.pipe';

describe('WorkOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
